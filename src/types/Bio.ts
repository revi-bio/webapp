export interface Bio {
  name: string,
  _id?: string,
  avatar?: string,
  backgroundImage?: string;
  handle: string,
  views: number,
  widgetsCount: number,
  pagesCount: number,
  createdAt: string,
  updatedAt: string
}
